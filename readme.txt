#for windows set PATH
setx PATH <msys/mingw/bin>; <qt/mingw/bin>
qmake
mingw32-make

#
Программа позволяет задать случайное количество точек,
сохранить их в xml, загрузить их из xml.

Строит между 2 точками прямые так, чтобы они не пересекались/
Также есть возможность соединить все точки между собой(считает расстояние кривой линии). 